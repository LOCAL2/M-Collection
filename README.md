# 🎨 M-Onew Gallery

เว็บไซต์แกลเลอรี่รูปภาพพี่เอ็มออนิว พร้อมระบบอัปโหลดและอัปเดตแบบ Real-time

## ✨ ฟีเจอร์

- 📸 อัปโหลดรูปภาพทีละหลายไฟล์
- 🎯 รองรับ Drag & Drop
- ⚡ อัปเดตแบบ Real-time ด้วย WebSocket
- 🗑️ ลบรูปภาพได้
- 💾 เก็บข้อมูลในไฟล์ (ไม่ใช้ Database)
- ⚠️ มีข้อความเตือนเกี่ยวกับการลบรูปอัตโนมัติ

## 🚀 วิธีใช้งาน

### 1. ติดตั้ง Dependencies

```bash
npm install
```

### 2. รันโปรเจค (Backend + Frontend พร้อมกัน)

```bash
npm run dev
```

Server จะรันที่ `http://localhost:3001`
Frontend จะรันที่ `http://localhost:5173`

### 3. เปิดเว็บไซต์

เปิดเบราว์เซอร์ไปที่ `http://localhost:5173`

## 📝 การใช้งาน

1. **อัปโหลดรูป**: คลิกที่กล่องอัปโหลดหรือลากรูปมาวาง
2. **ดูรูป**: รูปจะแสดงในแกลเลอรี่แบบ Real-time
3. **ลบรูป**: เลื่อนเมาส์ไปที่รูปแล้วคลิกปุ่มถังขยะ

## 🔧 เทคโนโลยีที่ใช้

- **Frontend**: React + TypeScript + Vite + Tailwind CSS
- **Backend**: Express.js + WebSocket
- **File Upload**: Multer
- **Real-time**: WebSocket (ws)
- **Styling**: Tailwind CSS v4 with custom animations

## 📁 โครงสร้างโปรเจค

```
m-collection/
├── src/
│   ├── App.tsx          # หน้าหลัก (Tailwind CSS)
│   ├── index.css        # Global styles + Tailwind
│   └── main.tsx         # Entry point
├── server.js            # Backend API + WebSocket
├── uploads/             # โฟลเดอร์เก็บรูป (สร้างอัตโนมัติ)
├── vite.config.ts       # Vite + Tailwind config
└── package.json
```

## ⚠️ หมายเหตุ

- รูปภาพจะถูกเก็บในโฟลเดอร์ `uploads/`
- ไม่มีการใช้ Database (เก็บข้อมูลในหน่วยความจำและไฟล์)
- WebSocket จะเชื่อมต่อใหม่อัตโนมัติหากขาดการเชื่อมต่อ
